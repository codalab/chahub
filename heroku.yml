build:
  docker:
    web: Dockerfile
    postgres: docker/postgres/Dockerfile
    elastic-search: docker/elastic_search/Dockerfile
    builder: Dockerfile.builder
run:
  web: bash -c "cd /app/src && /usr/local/bin/gunicorn asgi:application -w 2 -k uvicorn.workers.UvicornWorker -b :8888 --reload"
