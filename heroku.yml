#setup:
#  addons:
#    - plan: heroku-postgresql
#      as: DATABASE
#  config:
#    S3_BUCKET: my-example-bucket
build:
  docker:
    web: Dockerfile
    postgres: postgres:9.6.3
    elastic-search: elasticsearch:6.6.0
    builder: Dockerfile.builder
#    worker: worker/Dockerfile
#  config:
#    RAILS_ENV: development
#    FOO: bar
#release:
#  command:
#    - ./deployment-tasks.sh
#  image: worker
run:
  web: bash -c "cd /app/src && /usr/local/bin/gunicorn asgi:application -w 2 -k uvicorn.workers.UvicornWorker -b :8888 --reload"
#  worker: python myworker.py
#  asset-syncer:
#    command:
#      - python asset-syncer.py
#    image: worker
